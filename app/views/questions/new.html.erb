<h1><%= @question_set.title %></h1>

<div class="center">
  <%= render 'form' %>

    <%= button_to 'Cancel', question_set_path(@question_set), method: 'get',
    :form => { :id => 'add_question_cancel' } %>
</div>

