# SQLite version 3.x
#   gem install postgresql
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'postgresql'
development:
  adapter: postgresql
  host: localhost
  database: shuntoo_development
  pool: 
  timeout: app-dev

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
  adapter: postgresql
  host: localhost
  database: shuntoo_test
  pool: 
  timeout: app-dev

production:
  adapter: postgresql
  database: 
  pool: 
  timeout: app-dev
